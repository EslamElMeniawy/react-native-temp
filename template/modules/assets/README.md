# @modules/assets

Static asset references (images, icons).

## Public API

- `AppImages` — Typed image asset references (require-based)

## Dependencies

None

## Constraints

- Must NOT import from domain, feature, navigation, or app layers
- Enforced by `eslint-plugin-boundaries` (tier: shared)
